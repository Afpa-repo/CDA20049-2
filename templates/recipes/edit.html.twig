{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('RecipesNew') }}
{% endblock %}

{% block title %}Edit Recipes{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Recipes</h1>

        {{ include('recipes/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn" href="{{ path('recipes_index') }}">back to list</a>

        {{ include('recipes/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('RecipesNew') }}
{% endblock %}
